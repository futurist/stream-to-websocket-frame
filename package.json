{
  "name": "stream-to-websocket-frame",
  "version": "1.0.4",
  "description": "Convert stream data into WebSocket Frame Data, text or binary, works both in Node.js and Browser.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "prepublishOnly": "echo 'build...'; npx tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/events": "^3.0.3",
    "typescript": "^5.5.2"
  },
  "dependencies": {
    "events": "^3.3.0"
  }
}
